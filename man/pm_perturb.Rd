% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/perturbation_mechanism.R
\name{pm_perturb}
\alias{pm_perturb}
\title{Perturb an aggregated table}
\usage{
pm_perturb(
  data,
  total,
  x1,
  ck_var,
  params,
  indicator = NULL,
  operation = "sum",
  x2 = NULL,
  reduce_key = FALSE,
  key_digits = 9L
)
}
\arguments{
\item{data}{A \code{data.frame} of aggregated data.}

\item{total, x1}{Column names (strings): the aggregate and the largest
contribution, in the same unit.}

\item{ck_var}{Column name of the aggregated cell key.}

\item{params}{A fully calibrated \code{pm_params} object.}

\item{indicator}{Name of the indicator; defaults to the \code{total} column name.}

\item{operation}{Aggregation name entering the hash (default \code{"sum"}).}

\item{x2}{Optional column name of the second largest contribution. When
given, the share \code{rho2 = X2 / Y} is added, enabling the scenario-II
assessment.}

\item{reduce_key}{If \code{TRUE}, reduce \code{ck_var} to its fractional part (use it
when the column holds the raw sum of individual keys, Definition 6). If
\code{FALSE} (default), keys must already lie in [0;1].}

\item{key_digits}{Decimals used to stringify the key before hashing
(default 9).}
}
\value{
\code{data} with added columns \code{rho}, \code{rho2} (when \code{x2} is given),
\code{ck_nu}, \code{ck_eps}, and the perturbed total named \verb{<total>_pert}.
Calibration parameters and hashing settings are attached as the \code{pm_meta}
attribute.
}
\description{
Applies the mechanism of Definition 2 to a table of sums:
\verb{Y' = Y (1 + rho^n nu + eps)} with \code{rho = X1 / Y}. The draws are obtained
from the cell keys via \code{\link[=pm_draws]{pm_draws()}}, so the perturbation is reproducible:
re-running on the same cells and the same indicator returns the same values.
}
\details{
The mechanism is defined for strictly positive totals. Cells with a
non-positive total are left unperturbed and flagged, with a warning: being
multiplicative, the noise protects neither zeros nor near-zero values.
}
\examples{
para <- pm_commit_dominance(pm_commit_diff(pm_params()), sigma_nu = 0.4, n = 4)
tab <- data.frame(turnover = c(1000, 500), x1 = c(850, 120), ck = c(0.12, 0.87))
pm_perturb(tab, total = "turnover", x1 = "x1", ck_var = "ck", params = para)
}
