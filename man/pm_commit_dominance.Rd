% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibration_dominance.R
\name{pm_commit_dominance}
\alias{pm_commit_dominance}
\title{Commit the dominance decision (sigma_nu and n) into the parameter object}
\usage{
pm_commit_dominance(
  params,
  sigma_nu,
  n,
  beta = NULL,
  tau = NULL,
  level = 0.95,
  rho = NULL
)
}
\arguments{
\item{params}{A \code{pm_params} object with \code{sigma_eps} set.}

\item{sigma_nu, n}{The chosen values (single values). Recall the calibration
rule of step 2: keep the \emph{largest} \code{n} whose worst-case risk still meets
the ceiling, so as to spare cells of intermediate dominance
(see \code{\link[=pm_calib_dominance]{pm_calib_dominance()}} and the trade-off map \code{\link[=pm_plot_tradeoff]{pm_plot_tradeoff()}}).}

\item{beta, tau}{Scenario-I accuracy threshold and risk ceiling. Default to
the \code{dominance} policy already stored in \code{params}.}

\item{level}{Confidence level for the reported CI loss (default 0.95).}

\item{rho}{Grid used to locate the worst case over rho.
Default \code{seq(0.001, 1, 0.001)}.}
}
\value{
The updated \code{pm_params}, invisibly.
}
\description{
Closes calibration step 2: records the chosen \code{sigma_nu} and \code{n}, together
with the scenario-I policy (\code{beta}, \code{tau}), into the \code{pm_params} object. This
is the single place where step 2 mutates \code{params}.
}
\details{
Requires \code{sigma_eps} to be already set (step 1), since both the scenario-I
risk and the information loss depend on it. The function reports the
resulting worst-case risk over \code{rho}, where that worst case occurs, and the
range of the information loss -- from its floor (rho -> 0, driven by
\code{sigma_eps} alone) to its ceiling (rho = 1, driven by \code{sigma_nu}). A warning
is raised when the worst-case risk exceeds \code{tau}: the values are still
recorded, the producer being free to accept the overshoot knowingly.
}
\examples{
para <- pm_commit_diff(pm_params())
para <- pm_commit_dominance(para, sigma_nu = 0.3, n = 6)
para
}
