% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/risk_utility_figures.R
\name{pm_plot_risk_profile}
\alias{pm_plot_risk_profile}
\title{Risk profile as a function of dominance}
\usage{
pm_plot_risk_profile(
  x,
  sigma_nu = NULL,
  n = NULL,
  beta = NULL,
  sigma_eps = NULL,
  scenario = NULL,
  thresholds = c(0.5, 0.8)
)
}
\arguments{
\item{x}{A calibration table (e.g. from \code{\link[=pm_calib_dominance]{pm_calib_dominance()}}).}

\item{sigma_nu, n, beta, sigma_eps, scenario}{Optional values to keep; \code{NULL}
keeps everything present in the table. Filtering is usually needed on
\code{beta} and \code{sigma_eps} to keep the panel grid readable.}

\item{thresholds}{Risk levels drawn as dashed horizontal lines.}
}
\value{
A \code{ggplot} object.
}
\description{
Reproduces Figure 2 of the paper, generalised: the disclosure risk against
the dominance level \code{rho}, one curve per shape parameter \code{n}, panels split by
\code{sigma_nu} (and by \code{scenario} when the table carries several).
}
\examples{
grid <- pm_calib_dominance(beta = c(0.1,0.2))
pm_plot_risk_profile(grid, sigma_nu = c(0.1, 0.2, 0.4, 0.5), beta = 0.2)
}
