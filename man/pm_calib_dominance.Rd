% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibration_dominance.R
\name{pm_calib_dominance}
\alias{pm_calib_dominance}
\title{Calibration of the dominance noise (sigma_nu and n)}
\usage{
pm_calib_dominance(
  params = NULL,
  sigma_nu = NULL,
  n = NULL,
  beta = NULL,
  sigma_eps = NULL,
  level = 0.95,
  rho = NULL
)
}
\arguments{
\item{params}{Optional \code{pm_params}. When supplied, \code{sigma_eps} and \code{beta}
default to its values.}

\item{sigma_nu, n}{Numeric vectors of candidate values. Default to
\code{seq(0.05, 0.5, 0.05)} and \code{seq(3, 12, 0.5)}.}

\item{beta}{Accuracy threshold(s) of scenario I. Default to the \code{dominance}
policy of \code{params}, or to \code{c(0.05, 0.1, 0.15, 0.2, 0.25)} (as in the
differencing step).}

\item{sigma_eps}{Fixed differencing noise. Default to
\code{params$mechanism$sigma_eps}, or \code{0} when no \code{params} is given.}

\item{level}{Confidence level for the CI loss metric (default 0.95).}

\item{rho}{Internal grid of dominance levels used to locate the worst case.
Default \code{seq(0, 1, 0.02)}.}
}
\value{
A \code{data.frame} of class \code{pm_calib_dominance}, one row per
\verb{(sigma_nu, n, beta, sigma_eps)}, with columns \code{risk_max}, \code{rho_at_max},
\code{EZ_max}, \code{CI_max} (the last two in percent).
}
\description{
Theoretical, data-free. For every candidate \verb{(sigma_nu, n, beta, sigma_eps)},
evaluates the scenario-I risk over an internal grid of dominance levels
\code{rho}, keeps its worst case (max over rho), and reports the maximum
information loss (reached at rho = 1, hence set by \code{sigma_nu} alone). The
returned table is the decision surface of step 2: feed it to \code{\link[=plot]{plot()}} for the
risk heat-map, to \code{\link[=pm_suggest_n]{pm_suggest_n()}} for the largest admissible \code{n}, then lock
the choice with \code{\link[=pm_commit_dominance]{pm_commit_dominance()}}.
}
\details{
Like \code{\link[=pm_calib_diff]{pm_calib_diff()}}, calling it with everything at \code{NULL} returns a
default exploration grid.
}
\examples{
# default exploration
grid <- pm_calib_dominance()
grid
summary(grid)

# at a committed sigma_eps
para <- pm_commit_diff(pm_params())
grid <- pm_calib_dominance(para)
}
