% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utility_metrics.R
\name{assess_loss_ci}
\alias{assess_loss_ci}
\title{Confidence-interval bound of the relative loss Z}
\usage{
assess_loss_ci(rho, sigma_nu, sigma_eps, n, level = 0.95)
}
\arguments{
\item{rho}{Dominance share X1/Y in (0,1]. Recycled.}

\item{sigma_nu, sigma_eps, n}{Mechanism parameters. Recycled.}

\item{level}{Confidence level (default 0.95).}
}
\value{
The upper bound L(rho) as a relative quantity.
}
\description{
Upper bound \verb{L(rho) = q * sqrt(rho^(2n) sigma_nu^2 + sigma_eps^2)} of the
level-\code{level} confidence interval \verb{[-L(rho), L(rho)]} of Z (Definition 4 of
the paper), with \code{q} the \code{(1+level)/2} quantile of the standard normal. The
interval is symmetric, so this single value gives both bounds.
}
\examples{
assess_loss_ci(rho = 1, sigma_nu = 0.3, sigma_eps = 0.03, n = 6)
}
