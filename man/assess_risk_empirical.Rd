% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/empirical_assessment.R
\name{assess_risk_empirical}
\alias{assess_risk_empirical}
\title{Observed disclosure risk}
\usage{
assess_risk_empirical(x, scenario = c("I", "II"), beta = NULL, by = NULL)
}
\arguments{
\item{x}{A table returned by \code{\link[=pm_perturb]{pm_perturb()}}.}

\item{scenario}{\code{"I"} (default) or \code{"II"}.}

\item{beta}{Accuracy threshold. Defaults to the matching policy of the
parameter object.}

\item{by}{Optional column name(s) to break the summary down by.}
}
\value{
A \code{data.frame} with the number of cells, the observed success rate
(in percent), the mean theoretical risk, and the maximum theoretical risk.
}
\description{
For each cell, evaluates whether the attack of a given scenario would in fact
have succeeded on the realised perturbation -- scenario I:
\verb{|Y' - X1| / X1 < beta}; scenario II: \verb{|(Y' - X2) - X1| / X1 < beta} -- and
compares the observed success rate with the theoretical probability
\code{mu(rho)} averaged over the table. The two should agree: the realised rate is
one draw from the probability the mechanism guarantees.
}
\details{
Scenario II requires the \code{x2} column to have been declared in \code{\link[=pm_perturb]{pm_perturb()}},
which adds the \code{rho2} share the theoretical measure needs.
The differencing scenario is not assessed here: it bears on pairs of cells,
not on single cells, and is controlled a priori by the bound of Proposition 7.
}
