% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/risk_utility_figures.R
\name{pm_plot_tradeoff}
\alias{pm_plot_tradeoff}
\title{Risk-utility trade-off map}
\usage{
pm_plot_tradeoff(
  x,
  loss = c("EZ", "CI"),
  sigma_nu = NULL,
  n = NULL,
  beta = NULL,
  sigma_eps = NULL,
  scenario = NULL,
  rho_marks = c(0.8, 0.9, 0.95),
  thresholds = c(0.5, 0.8)
)
}
\arguments{
\item{x}{A calibration table (e.g. from \code{\link[=pm_calib_dominance]{pm_calib_dominance()}}).}

\item{loss}{Loss metric on the x-axis: \code{"EZ"} (mean absolute loss, default)
or \code{"CI"} (upper confidence bound).}

\item{sigma_nu, n, beta, sigma_eps, scenario}{Optional values to keep.}

\item{rho_marks}{Dominance levels highlighted with a symbol.}

\item{thresholds}{Risk levels drawn as dashed horizontal lines.}
}
\value{
A \code{ggplot} object.
}
\description{
Reproduces Figure 4 of the paper: for each \verb{(sigma_nu, n)} the curve traces
the (information loss, risk) couple as \code{rho} runs over ]0;1]. Loss on the
x-axis, risk on the y-axis, colour by \code{sigma_nu}, panels by \code{n}, and open
symbols marking a few reference dominance levels.
}
\details{
Generalised on the choice of the risk metric only: pick the scenario with
\code{scenario} when the table carries several, since a single risk column can be
plotted at a time.
}
\examples{
grid <- pm_calib_dominance(sigma_eps = 0.031, beta = 0.2)
pm_plot_tradeoff(grid)
}
