% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/risk_utility_figures.R, R/suggest_n.R
\name{pm_plot_risk_max}
\alias{pm_plot_risk_max}
\title{Worst-case risk as a function of sigma_nu}
\usage{
pm_plot_risk_max(
  x,
  x_axis = c("sigma_nu", "n"),
  sigma_nu = NULL,
  n = NULL,
  beta = NULL,
  sigma_eps = NULL,
  scenario = NULL,
  tau = NULL,
  mark_frontier = TRUE,
  marks = NULL,
  thresholds = c(0.5, 0.8)
)

pm_plot_risk_max(
  x,
  x_axis = c("sigma_nu", "n"),
  sigma_nu = NULL,
  n = NULL,
  beta = NULL,
  sigma_eps = NULL,
  scenario = NULL,
  tau = NULL,
  mark_frontier = TRUE,
  marks = NULL,
  thresholds = c(0.5, 0.8)
)
}
\arguments{
\item{x}{A calibration table (e.g. from \code{\link[=pm_calib_dominance]{pm_calib_dominance()}}).}

\item{x_axis}{Parameter on the x-axis: \code{"sigma_nu"} (default) or \code{"n"}.}

\item{sigma_nu, n, beta, sigma_eps, scenario}{Optional values to keep.}

\item{tau}{Risk ceiling, drawn as a dashed line. Defaults to \code{thresholds}.}

\item{mark_frontier}{If \code{TRUE} and \code{x_axis = "n"}, mark the largest
admissible \code{n} of each \code{sigma_nu} with a point. Requires a single \code{tau}.}

\item{marks}{Values of the x-axis parameter highlighted with a point;
\code{NULL} for none.}

\item{thresholds}{Risk levels drawn as dashed horizontal lines.}
}
\value{
A \code{ggplot} object.

A \code{ggplot} object.
}
\description{
Reproduces the middle panel of Figure 3, generalised: the worst case over
\code{rho} of the disclosure risk against \code{sigma_nu}, one curve per \code{n}, at a
fixed \code{sigma_eps}. Panels split by \code{scenario} when several are present.

Reduces a calibration table to the worst case over \code{rho} and plots it against
one of the two mechanism parameters.
}
\details{
With \code{x_axis = "sigma_nu"} this is the middle panel of Figure 3: the risk
decreases with \code{sigma_nu}, one curve per \code{n}.

With \code{x_axis = "n"} it is the view that accompanies \code{\link[=pm_suggest_n]{pm_suggest_n()}}: the risk
increases monotonically with \code{n}, one curve per \code{sigma_nu}. Each crossing of
the ceiling \code{tau} is the \code{n_max} of its \code{sigma_nu} -- the very root that
\code{\link[=pm_suggest_n]{pm_suggest_n()}} solves for. Set \code{mark_frontier = TRUE} to overlay those
points, computed by the same function so the plot and the table always agree.
}
\examples{
grid <- pm_calib_dominance(sigma_nu = seq(0.01, 0.5, 0.005), n = c(3, 6, 9, 12))
pm_plot_risk_max(grid, beta = 0.2)
grid <- pm_calib_dominance(sigma_eps = 0.031, beta = 0.2,
                           sigma_nu = c(0.3, 0.4, 0.5), n = seq(1, 12, 0.2))
pm_plot_risk_max(grid, x_axis = "n", tau = 0.5)
}
