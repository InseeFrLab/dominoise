% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/suggest_n.R
\name{pm_suggest_n}
\alias{pm_suggest_n}
\title{Largest admissible shape parameter n}
\usage{
pm_suggest_n(
  params = NULL,
  sigma_nu = NULL,
  beta = NULL,
  tau = NULL,
  sigma_eps = NULL,
  margin = 0,
  n_range = c(1, 30),
  integer = FALSE,
  level = 0.95,
  rho = NULL
)
}
\arguments{
\item{params}{Optional \code{pm_params}; supplies \code{sigma_eps}, \code{beta} and \code{tau}.}

\item{sigma_nu}{Numeric vector of candidate values. Default
\code{seq(0.05, 0.5, 0.05)}.}

\item{beta, tau}{Scenario-I threshold and ceiling. Default to the \code{dominance}
policy of \code{params}.}

\item{sigma_eps}{Fixed differencing noise. Defaults to
\code{params$mechanism$sigma_eps}.}

\item{margin}{Safety margin in [0,1): the effective ceiling is
\code{tau * (1 - margin)}. \code{n_max} is a boundary solution with zero slack, so a
small margin guards against later revisions of the policy. Default 0.}

\item{n_range}{Search interval for \code{n}. Default \code{c(1, 30)}.}

\item{integer}{If \code{TRUE}, round \code{n_max} down to an integer -- the only
conservative rounding, the risk being increasing in \code{n}.}

\item{level}{Confidence level for the reported CI loss (default 0.95).}

\item{rho}{Grid used to locate the worst case. Default \code{seq(0.001, 1, 0.001)}.}
}
\value{
A \code{data.frame} with one row per \code{sigma_nu}: \code{n_max},
\code{risk_at_n_max}, and the information loss at \code{rho = 1} and at the dominance
threshold \code{1 - beta}. \code{n_max} is \code{NA} when no \code{n} meets the ceiling (raise
\code{sigma_nu}), and the upper end of \code{n_range} when the whole range qualifies.
}
\description{
Implements the step-2 calibration rule. The worst-case scenario-I risk
increases with \code{n}, while the information loss decreases with \code{n} at every
\code{rho < 1} and is unchanged at \code{rho = 1}. Both objectives are therefore
monotone and opposite in \code{n}: there is no interior optimum, and the best
choice is the upper edge of the admissible set,
\verb{n_max = sup\{n : max_rho mu_I <= tau\}}. Because the risk is monotone, that
edge is found by root-finding rather than by a grid search.
}
\details{
The rule holds whatever the dominance profile of the table: since utility
improves at every \code{rho}, no weighting by the actual distribution of \code{rho}
could favour a smaller \code{n}.

Note this determines \code{n} \emph{given} \code{sigma_nu}. Every point of the returned
frontier meets the ceiling exactly, so choosing among them is a pure utility
arbitrage -- larger \code{sigma_nu} costs more at \code{rho = 1} but spares cells of
intermediate dominance. The reported losses are there to settle it.
}
\examples{
para <- pm_commit_diff(pm_params())
pm_suggest_n(para, sigma_nu = c(0.3, 0.4, 0.5))
}
