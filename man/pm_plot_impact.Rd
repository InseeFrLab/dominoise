% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_impact.R
\name{pm_plot_impact}
\alias{pm_plot_impact}
\title{Observed perturbation against dominance}
\usage{
pm_plot_impact(x, level = 0.95, colour_by = NULL, alpha = 0.35, n_grid = 200)
}
\arguments{
\item{x}{A table returned by \code{\link[=pm_perturb]{pm_perturb()}}.}

\item{level}{Confidence level of the band (default 0.95).}

\item{colour_by}{Optional column name used to colour the points (e.g. a
publication stratum).}

\item{alpha}{Point transparency; lower it on large tables.}

\item{n_grid}{Number of dominance values used to draw the envelope.}
}
\value{
A \code{ggplot} object.
}
\description{
Plots the relative deviation actually undergone by each cell against its
dominance level, and overlays the theoretical envelope predicted before any
data were touched: the level-\code{level} confidence band of the relative loss
(Definition 4) and the mean absolute loss (Proposition 3).
}
\details{
The mechanism being analytical, the cloud of points should fill the band and
straddle the mean lines. That agreement is the natural consistency check
between what was promised at calibration time and what the table received.
}
\examples{
set.seed(123)
N = 1000
params <- pm_commit_dominance(pm_commit_diff(pm_params()), sigma_nu = 0.4, n = 4)
rhos <- runif(N, 0, 1)
tab <- data.frame(turnover = runif(N, 0,1000), ck = runif(N, 0, 1))
tab$x1 <- tab$turnover * rhos
res <- pm_perturb(tab, "turnover", "x1", "ck", params)
pm_plot_impact(res)
}
