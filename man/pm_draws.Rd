% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/perturbation_mechanism.R
\name{pm_draws}
\alias{pm_draws}
\title{Derive the two Gaussian draws from a cell key}
\usage{
pm_draws(ck, params, indicator, operation = "sum", key_digits = 9L)
}
\arguments{
\item{ck}{Numeric vector of cell keys in [0;1].}

\item{params}{A fully calibrated \code{pm_params} object.}

\item{indicator}{Name of the indicator the aggregate is computed from
(e.g. \code{"turnover"}). Enters the hashed string.}

\item{operation}{Name of the aggregation (default \code{"sum"}). Enters the
hashed string, so that several statistics on the same indicator get
independent draws.}

\item{key_digits}{Number of decimals used to turn the key into a string.
Fixed for reproducibility; changing it changes every draw.}
}
\value{
A \code{data.frame} with columns \code{ck_nu}, \code{ck_eps}, \code{nu}, \code{eps}.
}
\description{
Implements Definitions 6-7 of the paper. From the cell key \code{CK}, the name of
the indicator and the aggregation operation, two distinct strings are built
-- one per draw -- and hashed with SHA-512. Each digest is mapped to a
uniform draw in ]0;1[, then turned into a Gaussian draw by quantile
inversion: \code{nu} from N(0, sigma_nu^2) and \code{eps} from N(0, sigma_eps^2).
}
\details{
Determinism, avalanche effect and uniformity of SHA-512 (Proposition 8)
ensure that the same cell always receives the same perturbation, while draws
remain uncorrelated across indicators and between \code{nu} and \code{eps}.
}
\examples{
para <- pm_commit_dominance(pm_commit_diff(pm_params()), sigma_nu = 0.4, n = 4)
pm_draws(c(0.12, 0.87), para, indicator = "turnover")
}
