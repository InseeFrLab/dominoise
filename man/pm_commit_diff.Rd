% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calibration_differencing.R
\name{pm_commit_diff}
\alias{pm_commit_diff}
\title{Commit the differencing decision into the parameter object}
\usage{
pm_commit_diff(
  params,
  beta = params$policy$diff$beta,
  tau = params$policy$diff$tau,
  level = 0.95
)
}
\arguments{
\item{params}{A \code{pm_params} object.}

\item{beta, tau}{The chosen threshold and ceiling. Default to the values
already stored in the \code{diff} policy. Must be single values.}

\item{level}{Confidence level used only for the informative message.}
}
\value{
The updated \code{pm_params} (returned invisibly).
}
\description{
Once \code{beta_diff} and \code{tau_diff} are chosen (typically after exploring with
\code{\link[=pm_calib_diff]{pm_calib_diff()}}), this fills \code{sigma_eps} in the \code{pm_params} object and
records the two policy values. It is the single place where step 1 mutates
\code{params}.
}
\examples{
parameters <- pm_params()

# beta and tau already set in the pm_params object:
parameters <- pm_commit_diff(parameters)
parameters

# if an another choice is preferred (after calibration search)
parameters <- pm_commit_diff(parameters, beta = 0.1, tau = 0.9)
parameters
}
